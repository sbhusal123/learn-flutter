# Error Handling


```dart
import 'package:http/http.dart';
import 'dart:convert';

class WorldTime {
  // ... existing properties

  Future<void> getTime() async{

    // The code inside try block will execute. If it throws any error, 
    // the code inside catch block will execute.
    try {
      Response response = await get(Uri.parse('https://time.now/developer/api/timezone/$url'));

      Map data = jsonDecode(response.body);

      // ... existing code to parse time

      time = now.toString();
    } catch(e) {
      // print error
      print('ERROR Getting time: $e');

      time = 'could not get time';
    }
  }
}
```
